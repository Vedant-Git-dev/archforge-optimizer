"""Unit tests for the adapter kit (`archforge.host.adapters`, `spec_builder`, `runner`).

Pure + deterministic + no SDK — these join the default suite. The
*reference-adapter* proof (a real adapter built on the kit) lives in
`LuminaAI/archforge_glue/test_smoke_offline.py`, outside `testpaths`.
"""
